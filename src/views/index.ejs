<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>index.ejs</title>
</head>
<body>
    <h1>Mis Posteos</h1>
    <a href="/create">New Post</a>
    <ul>
        <% allPosts.forEach((post) => { %>
            <li>
                <p>
                    <b><%= post.title %></b><br>
                    <i><%= post.content %></i><br>
                    <img src="<%= post.imglink %>" alt="%= post.title %>"><br>
                    <i><%= post.createdAt %></i>

                    
                </p>
            </li>


        <% })
        %>

    </ul>
    
</body>
</html>