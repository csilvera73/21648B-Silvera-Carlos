<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>index.ejs</title>
</head>
<body>
    <a href="/posts/create">Crear nuevo Post</a>
    <h1>Mis Posteos</h1>
    <ul>
        <% allPosts.forEach((post) => { %>
            <li>
                <p>
                    <b><%= post.title %></b><br>
                    <i><%= post.content %></i><br>
                    <img src="<%= post.imglink %>" alt="%= post.title %>"><br>
                    <i><%= post.createdAt %></i><br>
                   <a href="/posts/update/ <%= post.id %>">✏️ Modificar</a>
                   <a href="/posts/delete/ <%= post.id %>">❌ Eliminar</a>

                    
                </p>
            </li>


        <% })
        %>

    </ul>
    
</body>
</html>